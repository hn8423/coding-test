function solution(n, arr1, arr2) {
  var answer = [];
  let one = arr1.map((v) => {
    return v.toString(2);
  });
  let two = arr2.map((v) => {
    return v.toString(2);
  });
  let test = [39, 21].map((v) => {
    return v.toString(2);
  });
  return test;
}
